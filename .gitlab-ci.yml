variables:
  DOCKER_HOST: tcp://localhost:27879
  DOCKER_TLS_CERTDIR: ""

stages:
  - build
  - test

make:
    tags:
    - cudnn-frontend-runner
    stage: build
    script: "docker build -t cudnn_frontend  . "
    
run:
    tags:
    - cudnn-frontend-runner
    stage: test
    script: "ls"